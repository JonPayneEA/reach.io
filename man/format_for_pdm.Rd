% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{format_for_pdm}
\alias{format_for_pdm}
\title{Format a time series for PDM for PCs software}
\usage{
format_for_pdm(dt, measure = c("rainfall", "flow", "level"), tz = "GMT")
}
\arguments{
\item{dt}{A \code{data.table} or \code{data.frame} with at least two columns:
\code{dateTime} (POSIXct) and \code{value} (numeric).}

\item{measure}{Character. One of \code{"rainfall"}, \code{"flow"}, or \code{"level"}.
Names the data column in the output.}

\item{tz}{Character. Time zone used when decomposing \code{dateTime} into its
components. Defaults to \code{"GMT"}.}
}
\value{
A plain \code{data.frame} (not a \code{data.table}) with columns
\code{year}, \code{month}, \code{day}, \code{hour}, \code{minute}, \code{second}, and \verb{<measure>}.
}
\description{
Reshapes a \code{data.table} (or \code{data.frame}) containing a \code{dateTime} and
\code{value} column into the column layout expected by the PDM for PCs
rainfall-runoff modelling software:
}
\details{
\code{year | month | day | hour | minute | second | <measure>}

The measure column is named \code{"rainfall"}, \code{"flow"}, or \code{"level"} according
to the \code{measure} argument.
}
\examples{
library(data.table)
dt <- data.table(
  dateTime = as.POSIXct("2024-03-15 09:30:00", tz = "GMT"),
  value    = 2.5
)
format_for_pdm(dt, "rainfall")
}
