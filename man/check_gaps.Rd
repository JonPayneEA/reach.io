% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{check_gaps}
\alias{check_gaps}
\title{Identify missing timesteps for a Bronze gauge series}
\usage{
check_gaps(
  root,
  site_id,
  data_type,
  category = "hydrometric",
  expected_interval = NULL
)
}
\arguments{
\item{root}{Character. Root of the data store.}

\item{site_id}{Character. Site identifier.}

\item{data_type}{Character. Framework data type code (\code{"Q"}, \code{"H"},
\code{"P"}, etc.).}

\item{category}{Character. Data category subdirectory. Default
\code{"hydrometric"}.}

\item{expected_interval}{Character or \code{NULL}. Expected time step between
observations: \code{"15min"}, \code{"hourly"}, or \code{"daily"}. When \code{NULL}
(default), the interval is inferred from the register if available,
otherwise estimated from the observed median gap between successive
timestamps.}
}
\value{
A \code{data.table} with columns \code{gap_start} (POSIXct), \code{gap_end}
(POSIXct), and \code{n_missing} (integer). An empty table means no gaps were
found. Also prints a summary header. Returned invisibly.
}
\description{
Reads all Bronze Parquet files for the specified \code{site_id} and \code{data_type},
deduplicates timestamps (which may overlap across ingest batches), and
identifies any missing timesteps given the expected observation interval.
}
\details{
Gaps are reported as a \code{data.table} of contiguous missing periods, each
described by its \code{gap_start}, \code{gap_end}, and \code{n_missing} (number of missing
timesteps within that period).
}
\examples{
\dontrun{
# Check for gaps in 15-min flow record
check_gaps("data/hydro", site_id = "39001", data_type = "Q")

# Force daily interval check on a stage gauge
check_gaps("data/hydro", site_id = "2723TH", data_type = "H",
           expected_interval = "daily")
}
}
