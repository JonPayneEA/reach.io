% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silver.R
\name{qc_p_dry_run}
\alias{qc_p_dry_run}
\title{Detect extended dry runs during known wet periods (standalone utility)}
\usage{
qc_p_dry_run(value, wet_period_mask, dry_run_min_steps = 12L)
}
\arguments{
\item{value}{Numeric vector of 15-min rainfall increments (mm),
ordered by time.}

\item{wet_period_mask}{Logical vector, same length as \code{value}.
\code{TRUE} indicates the timestep falls within a known wet period.}

\item{dry_run_min_steps}{Integer. Minimum consecutive zero-rainfall steps
during a wet period to trigger the flag. Default 12 (three hours).}
}
\value{
Logical vector; \code{TRUE} throughout each flagged dry run.
}
\description{
Long sequences of zero rainfall during periods when neighbouring gauges
are recording rain suggest a blocked or frozen gauge. This check requires
an external \code{wet_period_mask} (a logical vector of the same length
as \code{value}, \code{TRUE} where the period is known to be wet) and is
therefore not applied automatically inside \code{promote_to_silver()}.
}
