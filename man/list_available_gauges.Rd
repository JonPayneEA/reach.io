% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{list_available_gauges}
\alias{list_available_gauges}
\title{List all gauges available in the Bronze store}
\usage{
list_available_gauges(root, category = NULL, supplier = NULL, data_type = NULL)
}
\arguments{
\item{root}{Character. Root of the data store (the directory containing
\verb{bronze/}, \verb{register/}, etc.).}

\item{category}{Character vector or \code{NULL}. Filter to one or more data
categories, e.g. \code{"hydrometric"}, \code{"radarH19"}, \code{"MOSES"}. \code{NULL}
(default) returns all categories.}

\item{supplier}{Character vector or \code{NULL}. Filter to one or more supplier
codes, e.g. \code{"EA"}, \code{"WISKI"}. \code{NULL} (default) returns all suppliers.}

\item{data_type}{Character vector or \code{NULL}. Filter to one or more
framework data type codes, e.g. \code{"Q"}, \code{"H"}, \code{"P"}. \code{NULL} (default)
returns all types.}
}
\value{
A \code{data.table} with one row per unique site + data-type
combination, printed as a formatted catalogue. Returned invisibly.
Columns: \code{site_id}, \code{category}, \code{supplier_code}, \code{data_type},
\code{temporal_resolution}, \code{start}, \code{end}, \code{n_datasets}.
}
\description{
Reads the governance register and returns a catalogue of every gauge (unique
\code{site_id} + \code{data_type} combination) for which Bronze data has been
ingested.  Results can be filtered to a specific category, supplier, or
data type.
}
\details{
The register is the primary source of truth. If it does not yet exist, a
message is printed and an empty table is returned.
}
\examples{
\dontrun{
# All gauges
list_available_gauges("data/hydro")

# Hydrometric flow gauges from the EA only
list_available_gauges("data/hydro",
                      category  = "hydrometric",
                      supplier  = "EA",
                      data_type = "Q")
}
}
