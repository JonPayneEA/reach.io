% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_sine.R
\name{fit_pe_sine}
\alias{fit_pe_sine}
\title{Fit a sine curve to an observed PE series}
\usage{
fit_pe_sine(pe, dates, method = c("lm_fourier", "nls"))
}
\arguments{
\item{pe}{Numeric vector. Daily PE values (mm day\eqn{^{-1}}).
Must not contain NA.}

\item{dates}{Date vector. Dates corresponding to \code{pe}. Must be the same
length as \code{pe} and must not contain NA.}

\item{method}{Character. Fitting method: \code{"lm_fourier"} (default, stable)
or \code{"nls"} (direct nonlinear fit, may not converge for noisy series).}
}
\value{
A named list with elements:
\describe{
\item{\code{B}}{Annual mean PE (mm day\eqn{^{-1}}).}
\item{\code{A}}{Amplitude (mm day\eqn{^{-1}}).}
\item{\code{phi}}{Phase offset (day of year of peak PE).}
\item{\code{r_squared}}{Coefficient of determination of the sine fit.}
\item{\code{rmse}}{Root mean squared error (mm day\eqn{^{-1}}).}
\item{\code{method}}{The method actually used (may differ from \code{method} if
NLS fell back to \code{"lm_fourier"}).}
\item{\code{n_obs}}{Number of observations used in the fit.}
}
}
\description{
Estimates the three parameters of the annual PE sine model:
\deqn{PE(t) = B + A \sin\!\left(\frac{2\pi}{365.25}(doy - \phi)\right)}
where \eqn{B} is the annual mean (mm day\eqn{^{-1}}), \eqn{A} is the
amplitude (mm day\eqn{^{-1}}), and \eqn{\phi} is the phase offset
(day of year of peak PE; approximately 172 for UK lowland catchments,
i.e. around 21 June).
}
\details{
Two fitting methods are available. \code{"lm_fourier"} (the default) rewrites
the model as a linear regression via a Fourier identity, making it
numerically stable and guaranteed to converge. \code{"nls"} fits the model
directly using nonlinear least squares; if NLS fails to converge it falls
back to \code{"lm_fourier"} with a warning.

The sine approximation is reliable for UK lowland catchments. In upland or
coastal sites where PE timing is shifted by elevation or sea surface
temperature, inspect \code{r_squared} before using the fitted curve
operationally.
}
\examples{
# Synthetic noiseless series with known parameters
dates  <- seq(as.Date("2010-01-01"), as.Date("2014-12-31"), by = "day")
doy    <- as.integer(format(dates, "\%j"))
pe_obs <- 2.5 + 2.0 * sin(2 * pi / 365.25 * (doy - 172))

params <- fit_pe_sine(pe_obs, dates)
params$B    # ~2.5
params$A    # ~2.0
params$phi  # ~172

# NLS method
params_nls <- fit_pe_sine(pe_obs, dates, method = "nls")
}
