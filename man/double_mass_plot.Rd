% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/double_mass_plot.R
\name{double_mass_plot}
\alias{double_mass_plot}
\title{Double-mass plot for hydrological volume comparison (trial)}
\usage{
double_mass_plot(
  x_data,
  y_data,
  catchment_area_km2 = NULL,
  weights_x = NULL,
  weights_y = NULL,
  pe = NULL,
  output_units = "Ml",
  title = "Double-mass plot",
  subtitle = NULL,
  label_x = NULL,
  label_y = NULL
)
}
\arguments{
\item{x_data}{A \code{HydroData} or \code{PotEvapData} object, \strong{or} a list of
such objects, contributing to the X axis.}

\item{y_data}{A \code{HydroData} or \code{PotEvapData} object, \strong{or} a list of
such objects, contributing to the Y axis.}

\item{catchment_area_km2}{Numeric scalar. Catchment area in km².
Required when any source is rainfall or potential evaporation (both
measured in mm and must be multiplied by area to give volume).}

\item{weights_x}{Numeric vector, length \code{length(x_data)}. Multiplicative
weights for each X source. Defaults to \code{1} for all sources.}

\item{weights_y}{Numeric vector, length \code{length(y_data)}. Multiplicative
weights for each Y source. Defaults to \code{1} for all sources.}

\item{pe}{An optional \link{PotEvap_Daily} object. When provided, the PE
signal is subtracted from every rainfall source on both axes before
any volume scaling or weighting is applied.}

\item{output_units}{Character scalar. Volume units for both axes.
\code{"m3"} (cubic metres), \code{"Ml"} (megalitres, default), or \code{"Gl"}
(gigalitres).}

\item{title}{Character scalar. Plot title.}

\item{subtitle}{Character scalar. Defaults to the overlapping date
range of the aligned data.}

\item{label_x, label_y}{Axis labels. Auto-generated from the source
parameter types and \code{output_units} when \code{NULL}.}
}
\value{
A \code{ggplot} object. The aligned, cumulative \code{data.table} used
to build the plot is attached as \code{attr(p, "data")} for inspection or
further processing.
}
\description{
Builds two cumulative volume series — one for each axis — from any
combination of \link{Rainfall_Daily}, \link{Rainfall_15min}, \link{Flow_Daily},
\link{Flow_15min}, \link{PotEvap_Daily}, \link{PotEvap_Hourly}, or \link{PotEvap_15min}
objects, then plots the cumulative X series against the cumulative Y
series. A perfectly consistent volume ratio plots as a straight line.
}
\details{
Dashed reference lines are drawn at 1:1 and at ±(final Y − final X)
to give a visual sense of overall balance and drift.
}
\section{PE subtraction}{

When \code{pe} is supplied, the daily PE signal is subtracted from every
rainfall source on both axes before volume conversion. The PE value is
distributed evenly across sub-daily rainfall intervals (e.g. ÷96 for
15-minute data).
}

\section{Mixed timesteps}{

All sources on the same axis should ideally share the same timestep.
Sources with differing timesteps are outer-joined by \code{dateTime}; rows
present in only one source contribute 0 via \code{na.rm = TRUE} in the
column sum, which may not be physically correct.
}

\section{Not yet supported}{

Level gauge data requires rating equations and is not currently
handled. Convert level to flow before passing data to this function.
}

\examples{
\dontrun{
# Daily flow vs weighted rainfall (no PE correction)
p <- double_mass_plot(
  x_data             = flow_obj,
  y_data             = list(rain_a, rain_b),
  catchment_area_km2 = 120.5,
  weights_y          = c(0.6, 0.4),
  output_units       = "Ml",
  title              = "Flow vs weighted rainfall"
)
p

# With PE correction applied to rainfall
p2 <- double_mass_plot(
  x_data             = flow_obj,
  y_data             = rain_obj,
  catchment_area_km2 = 120.5,
  pe                 = pe_daily_obj,
  output_units       = "Gl"
)
p2

# Inspect the underlying data
attr(p, "data")
}
}
