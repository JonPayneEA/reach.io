% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/output.R
\name{format_for_fmp}
\alias{format_for_fmp}
\title{Export flow time series to Flood Modeller Pro Bespoke Format 1}
\usage{
format_for_fmp(
  flows,
  title = "flow_export",
  comment = "",
  gauge_ids = NULL,
  ied_refs = NULL,
  start_time = NULL,
  na_fill = NA_real_,
  out_file = NULL
)
}
\arguments{
\item{flows}{A named \code{list} of \link{Flow_Daily} or \link{Flow_15min} objects, one per
gauge. A single (unnamed) object is also accepted and wrapped automatically.
List names are used to construct IED references when \code{ied_refs} is \code{NULL}.}

\item{title}{Character scalar. File title placed in Row 1. Default
\code{"flow_export"}.}

\item{comment}{Character scalar. Free-text comment placed in Row 2. Default
\code{""} (blank row).}

\item{gauge_ids}{Character vector, one element per site. WISKI gauge IDs
written to Row 3. If \code{NULL} (default), Row 3 is written with blank cells.}

\item{ied_refs}{Character vector, one element per site. IED reference labels
for Row 4. If \code{NULL} (default), derived from \code{names(flows)} and
\code{gauge_ids} according to the rules in the Details section.}

\item{start_time}{\code{POSIXct} scalar. Timestamp used as \eqn{t = 0} for the
relative time column. If \code{NULL} (default), the earliest \code{dateTime} value
across all \code{flows} is used.}

\item{na_fill}{Numeric scalar. Value substituted for \code{NA} readings in the
output. Defaults to \code{NA_real_} (written as a blank CSV cell). Set to \code{0}
if FMP requires non-missing values.}

\item{out_file}{Character scalar or \code{NULL}. If supplied, the formatted lines
are written to this path (parent directories created as needed) and the
path is returned invisibly. If \code{NULL} (default), a character vector of
lines is returned invisibly — useful for inspection and testing.}
}
\value{
If \code{out_file} is \code{NULL}, a character vector of CSV lines (invisibly).
If \code{out_file} is supplied, \code{out_file} as a character scalar (invisibly).
}
\description{
Writes one or more \link{Flow_Daily} or \link{Flow_15min} objects to the four-header-row
CSV format used by Flood Modeller Pro (FMP) to assign flow boundary conditions
to named 1D IED (node) references at simulation runtime.
}
\details{
\strong{File layout:}

\if{html}{\out{<div class="sourceCode">}}\preformatted{<title>,,                     <- Row 1: file title (first cell only)
<comment>,,                   <- Row 2: free-text comment (first cell only)
,<gauge_id_1>,<gauge_id_2>    <- Row 3: blank then WISKI gauge IDs
Time,<ied_ref_1>,<ied_ref_2>  <- Row 4: "Time" then IED references
0.000,12.500,8.100            <- data: relative hours, flow in m³/s
0.250,12.600,8.200
}\if{html}{\out{</div>}}

\strong{IED reference construction (Row 4):}
The column headers in Row 4 are resolved in order of preference:
\enumerate{
\item \code{ied_refs} supplied explicitly — used as-is.
\item \code{names(flows)} and \code{gauge_ids} both present — concatenated as
\code{"<name>_<gauge_id>"} (e.g. \code{"Bruton_405553"}).
\item Only \code{gauge_ids} supplied — gauge IDs used directly.
\item Only \code{names(flows)} present — list names used directly.
\item No metadata available — fallback labels \code{"site_1"}, \code{"site_2"}, …
}

\strong{Multi-site merging:}
When \code{flows} contains more than one object, all time series are merged by a
full outer join on \code{dateTime}. Sites with different date ranges or missing
timesteps produce \code{NA} (or \code{na_fill}) in the merged output rather than
silently truncating any series to the shortest.

\strong{Reference time (\code{start_time}):}
The time column is hours elapsed since \code{start_time}. By default this is the
earliest \code{dateTime} value across all input series, giving \code{t = 0} at the
first observation. Override \code{start_time} when the FMP simulation clock must
start before the data begins — for example, when supplying a warm-up period
or aligning multiple boundary files to a common origin.

\strong{In-memory vs. file output:}
Omitting \code{out_file} returns the formatted lines as a character vector,
which is convenient for inspection (\code{cat(lines, sep = "\\n")}) and unit
testing without touching the file system. Supply \code{out_file} to write
directly to disk; parent directories are created automatically.
}
\examples{
\dontrun{
# Single site
lines <- format_for_fmp(
  list(Bruton = flow_obj),
  title     = "Somerset_boundaries",
  comment   = "EA operational export 2024-01-15",
  gauge_ids = "405553"
)
cat(paste(lines[1:6], collapse = "\n"))

# Multiple sites — write directly to file
format_for_fmp(
  list(Bruton = flow1, Wincanton = flow2),
  title     = "Somerset_boundaries",
  gauge_ids = c("405553", "365943"),
  out_file  = "outputs/Somerset_boundaries.csv"
)
}
}
\seealso{
\code{\link[=format_for_pdm]{format_for_pdm()}} for PDM for PCs output; \code{\link[=apply_rating]{apply_rating()}} to
convert level data to flow before exporting.
}
