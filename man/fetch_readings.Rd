% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sync.R
\name{fetch_readings}
\alias{fetch_readings}
\title{Fetch readings for a single measure (synchronous API)}
\usage{
fetch_readings(
  measure_notation,
  from_date,
  to_date,
  limit = API_HARD_LIMIT,
  observation_type = NULL
)
}
\arguments{
\item{measure_notation}{Character. Unique measure ID — the \code{notation}
column from \code{\link[=get_measures]{get_measures()}}, e.g.
\code{"abc123-flow-m-86400-m3s-qualified"}.}

\item{from_date}{Character. Start date (inclusive) in \code{"YYYY-MM-DD"} format.}

\item{to_date}{Character. End date (exclusive) in \code{"YYYY-MM-DD"} format.}

\item{limit}{Integer. Maximum rows to return. Defaults to \code{API_HARD_LIMIT}
(2,000,000). Reduce for quick spot checks.}

\item{observation_type}{Character or \code{NULL}. \code{"Qualified"} for
quality-controlled data, \code{"Measured"} for raw. \code{NULL} returns both.}
}
\value{
A \code{data.table} of readings with columns \code{dateTime}, \code{date},
\code{value}, \code{quality}, \code{completeness}, and \code{measure_notation}. Returns
an empty \code{data.table} if no readings exist for the period.
}
\description{
Calls \verb{/hydrology/id/measures/\{measure\}/readings} and returns the result
as a \code{data.table}. The default API cap of 100,000 rows is overridden to
the hard maximum of 2,000,000. For larger requests use
\code{\link[=download_hydrology]{download_hydrology()}} with \code{method = "batch"}.
}
\examples{
\dontrun{
fetch_readings(
  "abc123-flow-m-86400-m3s-qualified",
  from_date = "2022-01-01",
  to_date   = "2022-12-31"
)
}
}
