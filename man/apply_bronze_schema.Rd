% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{apply_bronze_schema}
\alias{apply_bronze_schema}
\title{Apply the framework Bronze Parquet schema to a data.table}
\usage{
apply_bronze_schema(
  dt,
  dataset_id,
  site_id,
  data_type,
  timestamp_col = "dateTime",
  value_col = "value",
  supplier_flag_col = NULL,
  period = NA_character_
)
}
\arguments{
\item{dt}{A \code{data.table} of raw readings.}

\item{dataset_id}{Character. The Bronze dataset ID for this dataset.}

\item{site_id}{Character. Supplier site identifier.}

\item{data_type}{Character. Framework data type code.}

\item{timestamp_col}{Character. Name of the datetime column in \code{dt}.
Default \code{"dateTime"}.}

\item{value_col}{Character. Name of the value column in \code{dt}.
Default \code{"value"}.}

\item{supplier_flag_col}{Character or NULL. Name of the supplier quality
flag column in \code{dt}. NULL produces NA for all rows.}

\item{period}{Character or NA. Temporal resolution of the series, e.g.
\code{"15min"}, \code{"hourly"}, \code{"daily"}. Stored verbatim in the \code{period} column.
Default \code{NA_character_}.}
}
\value{
A \code{data.table} conforming to the Bronze schema.
}
\description{
Selects and coerces columns to match the mandatory Bronze schema defined
in Section 7.2 of the Hydrometric Data Framework. Input columns are
mapped from internal names to framework names. Any column not in the
schema is dropped.
}
\details{
Mandatory output columns:
\describe{
\item{\code{timestamp}}{POSIXct UTC. Full observation datetime, including
sub-daily time for instantaneous readings (15-min, hourly). For daily
aggregates (mean, max, min) the time component is midnight UTC — the
time-of-occurrence (e.g. time of peak flow) is not available from the
EA Hydrology API readings endpoint and must be derived from the
sub-daily series separately.}
\item{\code{value}}{float64. Observed value as received.}
\item{\code{supplier_flag}}{character. Quality code from supplier, or NA.}
\item{\code{dataset_id}}{character. Bronze dataset ID. Join key to register.}
\item{\code{site_id}}{character. Supplier site identifier.}
\item{\code{data_type}}{character. Framework code: Q, H, P, SM, SWE.}
\item{\code{period}}{character. Temporal resolution of the series, e.g.
\code{"15min"}, \code{"hourly"}, \code{"daily"}. \code{NA} when unknown.}
}
}
\examples{
\dontrun{
bronze_dt <- apply_bronze_schema(
  dt            = raw_dt,
  dataset_id    = "EA_39001_Q_20260115",
  site_id       = "39001",
  data_type     = "Q",
  timestamp_col = "dateTime",
  value_col     = "value",
  supplier_flag_col = "quality",
  period        = "15min"
)
}
}
