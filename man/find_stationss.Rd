% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup_old.R
\name{find_stationss}
\alias{find_stationss}
\title{Find stations matching a set of identifiers}
\usage{
find_stationss(
  names = NULL,
  wiski_ids = NULL,
  rloi_ids = NULL,
  notations = NULL,
  lat = NULL,
  long = NULL,
  dist = NULL
)
}
\arguments{
\item{names}{Character vector or \code{NULL}. Fuzzy station name search using
the API's \code{search} parameter.}

\item{wiski_ids}{Character vector or \code{NULL}. WISKI identifiers, e.g.
\code{"SS92F014"}.}

\item{rloi_ids}{Character vector or \code{NULL}. River Levels on the Internet
identifiers, e.g. \code{"5022"}.}

\item{notations}{Character vector or \code{NULL}. Station SUID/notation values,
e.g. \code{"c46d1245-e34a-4ea9-8c4c-410357e80e15"}.}

\item{lat}{Numeric or \code{NULL}. Latitude for proximity search. Must be
supplied with \code{long} and \code{dist}.}

\item{long}{Numeric or \code{NULL}. Longitude for proximity search.}

\item{dist}{Numeric or \code{NULL}. Search radius in km.}
}
\value{
A \code{data.table} with columns \code{label}, \code{notation}, \code{wiskiID},
\code{RLOIid}, \code{lat}, \code{long}, and \code{riverName} (where available). Returns
an empty \code{data.table} if no stations are found.
}
\description{
Looks up stations from the EA Hydrology API by one or more identifier
types and returns a \code{data.table} of matches. Multiple identifier types
can be supplied simultaneously — results are pooled and de-duplicated.
The returned table can be passed directly to \code{\link[=download_hydrology]{download_hydrology()}} via
the \code{wiski_ids}, \code{rloi_ids}, or \code{notations} arguments.
}
\examples{
\dontrun{
find_stations(names = "Thames")
find_stations(wiski_ids = c("SS92F014", "S11512_FW"))
find_stations(rloi_ids = c("5022", "7001"))
find_stations(notations = "c46d1245-e34a-4ea9-8c4c-410357e80e15")
find_stations(lat = 51.5, long = -1.8, dist = 10)

# Combine methods — results are pooled and de-duplicated
find_stations(wiski_ids = "SS92F014", lat = 51.5, long = -1.8, dist = 10)
}
}
