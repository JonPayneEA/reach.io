% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/silver.R
\name{promote_to_silver}
\alias{promote_to_silver}
\title{Promote a Bronze flow dataset to Silver tier with QC flags}
\usage{
promote_to_silver(
  bronze_data,
  output_dir,
  category = "hydrometric",
  allow_negative = FALSE,
  relative_spike_ratio = 10,
  min_baseline = 0.1,
  absolute_spike_k = 5,
  min_spike_flow = 1,
  drop_ratio = 0.9,
  min_flow_for_drop = 1,
  fluctuation_window = 8L,
  fluctuation_min_reversals = 4L,
  truncation_min_run = 4L,
  truncation_low_quantile = 0.1,
  truncation_high_quantile = 0.9,
  write_output = TRUE
)
}
\arguments{
\item{bronze_data}{Character path to a Bronze Parquet file, or a
\code{data.table} conforming to the Bronze schema.}

\item{output_dir}{Character. Root store directory. Silver file is written
to \verb{silver/<category>/<data_type>/<year>/<dataset_id>.parquet}.}

\item{category}{Character. Data category for the output path, e.g.
\code{"hydrometric"}. Default \code{"hydrometric"}.}

\item{allow_negative}{Logical. If \code{TRUE}, negative flow values are not
flagged as Rejected. Appropriate for ultrasonic or tidal gauges.
Default \code{FALSE}.}

\item{relative_spike_ratio}{Numeric. Ten-fold increase threshold for
relative spike detection. Default 10.}

\item{min_baseline}{Numeric (m3/s). Minimum preceding value required for
the relative spike check to fire. Default 0.1.}

\item{absolute_spike_k}{Numeric. MAD multiplier for absolute spike
detection. Default 5.}

\item{min_spike_flow}{Numeric (m3/s). Minimum current value for absolute
spike check to fire. Default 1.0.}

\item{drop_ratio}{Numeric (0–1). Fractional single-step decrease required
to flag a drop. Default 0.9.}

\item{min_flow_for_drop}{Numeric (m3/s). Minimum preceding value for drop
check to fire. Default 1.0.}

\item{fluctuation_window}{Integer. Rolling window size (timesteps) for
fluctuation detection. Default 8 (two hours).}

\item{fluctuation_min_reversals}{Integer. Minimum direction reversals in
the window to flag fluctuation. Default 4.}

\item{truncation_min_run}{Integer. Minimum consecutive identical values
to flag a truncation. Default 4 (one hour).}

\item{truncation_low_quantile}{Numeric (0–1). Quantile ceiling for
low-flow truncation. Default 0.1.}

\item{truncation_high_quantile}{Numeric (0–1). Quantile floor for
high-flow truncation. Default 0.9.}

\item{write_output}{Logical. If \code{TRUE} (default), write Silver Parquet to
disk. Set \code{FALSE} to return the result without writing.}
}
\value{
A \code{data.table} conforming to the Silver schema, invisibly when
\code{write_output = TRUE}.
}
\description{
Reads a Bronze Parquet file (or accepts a \code{data.table} directly), applies
the traditional quality-control checks from the UK-Flow15 framework
(Fileni et al., 2026), and writes a Silver Parquet file. The output
extends the Bronze schema with four QC columns: \code{qc_flag}, \code{qc_value},
\code{qc_y_code}, and \code{qc_flagged_at}.
}
\details{
QC checks are applied independently per \code{site_id}. The series for each
gauge is sorted by \code{timestamp} before checks are run.

Only the \strong{traditional QC checks} (Y-digit of the UK-Flow15 three-digit
code) are implemented here. The consistency checks against NRFA daily /
AMAX / POT data (X-digit) and the high-flow hydrological plausibility
checks (Z-digit) require external reference datasets and are not applied
automatically; they can be added to the output manually via the
\code{qc_x_code} and \code{qc_z_code} columns after this step.
}
\section{QC flag definitions}{

\describe{
\item{1 — Good}{No anomaly detected.}
\item{2 — Estimated}{Reserved for manually corrected or gap-filled
values; not assigned by this function.}
\item{3 — Suspect}{Anomaly detected but value may still be physically
plausible. Y-digit codes 2–7.}
\item{4 — Rejected}{Value is almost certainly erroneous. Y-digit codes
1 (negative), 8 (rel + abs spike), or 9 (drop + neg/trunc_low).}
}
}

\examples{
\dontrun{
# Promote a single Bronze file
promote_to_silver(
  bronze_data = "data/hydrometric/bronze/hydrometric/EA/Q/2024/EA_39001_Q_20240101.parquet",
  output_dir  = "data/hydrometric"
)

# From a data.table (e.g. after run_backfill())
silver_dt <- promote_to_silver(
  bronze_data  = bronze_dt,
  output_dir   = "data/hydrometric",
  write_output = FALSE
)

# Allow negative values for a tidal gauge
promote_to_silver(
  bronze_data    = "data/hydrometric/bronze/.../EA_TG001_Q_20240101.parquet",
  output_dir     = "data/hydrometric",
  allow_negative = TRUE
)
}
}
