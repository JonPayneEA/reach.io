% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{download_hydrology}
\alias{download_hydrology}
\title{Download hydrology readings from the EA Hydrology API}
\usage{
download_hydrology(
  parameters = c("rainfall", "flow", "level"),
  from_date = "2020-01-01",
  to_date = "2023-12-31",
  method = c("sync", "batch"),
  output = c("memory", "disk"),
  out_dir = NULL,
  period_name = "daily",
  value_type = NULL,
  observation_type = NULL,
  wiski_ids = NULL,
  rloi_ids = NULL,
  notations = NULL,
  chunk_by_year = TRUE,
  max_wait_s = 600
)
}
\arguments{
\item{parameters}{Character vector. Any combination of \code{"rainfall"},
\code{"flow"}, and \code{"level"}. Default is all three.}

\item{from_date}{Character. Start date (inclusive) in \code{"YYYY-MM-DD"} format.}

\item{to_date}{Character. End date (exclusive) in \code{"YYYY-MM-DD"} format.}

\item{method}{Character. \code{"sync"} (default) or \code{"batch"}.}

\item{output}{Character. \code{"memory"} (default) or \code{"disk"}.}

\item{out_dir}{Character or \code{NULL}. Root output directory. Required when
\code{output = "disk"}. Each parameter gets its own subdirectory, e.g.
\verb{out_dir/flow/}.}

\item{period_name}{Character. Temporal resolution: \code{"daily"} (default)
or \code{"15min"}.}

\item{value_type}{Character or \code{NULL}. Value statistic filter, e.g.
\code{"mean"}, \code{"max"}, \code{"total"}. \code{"all"} skips the filter. \code{NULL} uses
each parameter's built-in default.}

\item{observation_type}{Character or \code{NULL}. \code{"Qualified"} for
quality-controlled data, \code{"Measured"} for raw. \code{NULL} returns both.
Applies to \code{method = "sync"} only.}

\item{wiski_ids}{Character vector or \code{NULL}. Filter to stations with
these WISKI identifiers.}

\item{rloi_ids}{Character vector or \code{NULL}. Filter to stations with
these RLOIid identifiers.}

\item{notations}{Character vector or \code{NULL}. Filter to stations with
these notation (SUID) values.}

\item{chunk_by_year}{Logical. Split the date range into annual chunks?
Default \code{TRUE}. Applies to \code{method = "sync"} only.}

\item{max_wait_s}{Numeric. Maximum seconds to wait per batch job.
Default 600. Applies to \code{method = "batch"} only.}
}
\value{
In \code{"memory"} mode: a named list with one \code{data.table} per
requested parameter plus a \code{summary} \code{data.table} with columns
\code{parameter}, \code{measure}, \code{file}, \code{rows}, and \code{ok}. In \code{"disk"} mode:
the summary \code{data.table} invisibly, with CSVs written to \code{out_dir}.
}
\description{
Single entry point for downloading rainfall, flow, and/or level data from
the Environment Agency Hydrology API. At least one station identifier
argument (\code{wiski_ids}, \code{rloi_ids}, or \code{notations}) should normally be
supplied — omitting all three will attempt to download every station, which
is a very large request.
}
\details{
\subsection{Methods}{
\itemize{
\item \strong{\code{"sync"}} — Calls the synchronous readings endpoint directly. The
date range is split into annual chunks to stay within the API's hard
limit of 2,000,000 rows. Best for modest date ranges or small station
sets.
\item \strong{\code{"batch"}} — Submits requests to the asynchronous batch endpoint,
polls for completion, and downloads the resulting S3 CSVs. Better
suited to large bulk downloads where synchronous requests would time
out.
}
}

\subsection{Output modes}{
\itemize{
\item \strong{\code{"memory"}} — Returns a named list with one \code{data.table} per
parameter (e.g. \code{result$flow}, \code{result$rainfall}) plus a \code{summary}
element. No files are written.
\item \strong{\code{"disk"}} — Writes one CSV per measure into per-parameter
subdirectories under \code{out_dir} and returns the summary \code{data.table}
invisibly.
}

The EA API fair-use guidance asks that automated users issue only one
request at a time. Both methods are intentionally sequential.
}
}
\examples{
\dontrun{
# In-memory download by WISKI ID
result <- download_hydrology(
  parameters = c("flow", "level"),
  from_date  = "2022-01-01",
  to_date    = "2022-12-31",
  wiski_ids  = c("SS92F014", "S11512_FW")
)
result$flow
result$level
result$summary

# By RLOIid, to disk
download_hydrology(
  parameters = "flow",
  from_date  = "2022-01-01",
  to_date    = "2022-12-31",
  output     = "disk",
  out_dir    = "hydrology_data",
  rloi_ids   = c("5022", "7001")
)

# From find_stations() output
stns <- find_stations(names = "Avon")
result <- download_hydrology(
  parameters = "rainfall",
  from_date  = "2022-01-01",
  to_date    = "2022-12-31",
  wiski_ids  = stns$wiskiID
)

# Batch method, qualified data only
download_hydrology(
  parameters       = c("flow", "level"),
  from_date        = "2015-01-01",
  to_date          = "2023-12-31",
  method           = "batch",
  output           = "disk",
  out_dir          = "hydrology_data",
  observation_type = "Qualified",
  wiski_ids        = "SS92F014"
)
}
}
