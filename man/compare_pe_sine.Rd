% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_sine.R
\name{compare_pe_sine}
\alias{compare_pe_sine}
\title{Compare an observed PE series against a fitted sine curve}
\usage{
compare_pe_sine(pe, dates, B, A, phi, plot = FALSE)
}
\arguments{
\item{pe}{Numeric vector. Observed daily PE (mm day\eqn{^{-1}}).}

\item{dates}{Date vector. Dates corresponding to \code{pe}.}

\item{B}{Numeric. Annual mean parameter from \code{\link{fit_pe_sine}}.}

\item{A}{Numeric. Amplitude parameter from \code{\link{fit_pe_sine}}.}

\item{phi}{Numeric. Phase offset parameter from \code{\link{fit_pe_sine}}.}

\item{plot}{Logical. If \code{TRUE}, attach a \code{ggplot2} plot to the return
list as element \code{$plot}. The plot overlays thinned observed points
(grey) against the fitted sine line (blue) with RMSE and bias in the
subtitle. Default \code{FALSE}.}
}
\value{
A named list with elements:
\describe{
  \item{\code{rmse}}{Root mean squared error (mm day\eqn{^{-1}}).}
  \item{\code{bias}}{Mean signed error — fitted minus observed
    (mm day\eqn{^{-1}}). Positive values mean the model overestimates PE.}
  \item{\code{r_squared}}{Coefficient of determination.}
  \item{\code{fitted}}{\code{data.table} with columns \code{date}, \code{observed},
    \code{fitted}.}
  \item{\code{plot}}{A \code{ggplot} object. Only present when \code{plot = TRUE}.}
}
}
\description{
Evaluates the goodness-of-fit of a sine curve defined by parameters
\code{B}, \code{A}, \code{phi} against an observed PE series. Returns RMSE, bias,
and \eqn{R^2}, and optionally a diagnostic plot.

Inspect \code{r_squared} before using a fitted curve operationally —
values below ~0.85 suggest the catchment PE cycle is poorly described by a
simple sine (e.g. upland sites, coastal influence, or multi-modal
evaporation regimes).
}
\examples{
dates  <- seq(as.Date("2010-01-01"), as.Date("2019-12-31"), by = "day")
doy    <- as.integer(format(dates, "\%j"))
pe_obs <- 2.5 + 2.0 * sin(2 * pi / 365.25 * (doy - 172)) +
          stats::rnorm(length(dates), sd = 0.3)

params <- fit_pe_sine(pe_obs, dates)

# Numeric diagnostics only
diag <- compare_pe_sine(pe_obs, dates, params$B, params$A, params$phi)
diag$rmse
diag$bias
diag$r_squared

# With diagnostic plot
diag <- compare_pe_sine(pe_obs, dates, params$B, params$A, params$phi,
                        plot = TRUE)
diag$plot
}
\seealso{
\code{\link{fit_pe_sine}}, \code{\link{generate_pe_sine}}
}
