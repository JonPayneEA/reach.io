% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_store.R
\name{save_rating}
\alias{save_rating}
\title{Save a rating curve to the hydrometric ratings store}
\usage{
save_rating(x, ...)
}
\arguments{
\item{x}{A \link{RatingCurve} or \link{RatingSet}. The curve's \code{station_id} slot
must be set to a non-empty string.}

\item{root}{Character. Root of the data store
(the directory that contains \verb{bronze/}, \verb{silver/}, \verb{gold/},
\verb{register/}).}

\item{tier}{Character. Storage tier: \code{"bronze"}, \code{"silver"}, or
\code{"gold"} (default).}

\item{saved_by}{Character. Name or username of the person saving the
curve. Recorded in the register. Defaults to \code{Sys.info()[["user"]]}.}

\item{method_of_receipt}{Character. How the rating was obtained, e.g.
\code{"WISKI export"}, \code{"consultant report"}, \code{"manual entry"}. Recorded
in the register. Defaults to \code{NA}.}

\item{derivation_method}{Character. How the rating was developed.
Suggested values: \code{"spot gaugings"} (field measurements only),
\code{"hydraulic model"} (e.g. HEC-RAS, ISIS, TUFLOW), \code{"statistical model"},
or \code{"mixed"} (gaugings calibrated to a model). Free text is also
accepted. Defaults to \code{NA}.}

\item{model_or_project}{Character. Name of the hydraulic model, statistical
framework, project, or team that produced the rating (free text).
Useful when the same site has ratings from different sources over time.
Defaults to \code{NA}.}

\item{notes}{Character. Optional free-text note appended to the register
row (e.g. \code{"Post-survey 2023 refit — upper limb revised"}).}
}
\value{
The path(s) to the written Parquet file(s), invisibly.
}
\description{
Writes a \link{RatingCurve} (or every curve in a \link{RatingSet}) into the
shared \verb{<root>/<tier>/hydrometric/ratings/ratings.parquet} file and
appends a governance row to \verb{<root>/register/rating_register.csv}.
}
\details{
Three tiers are supported, matching the observation data lifecycle:
\describe{
\item{\code{"bronze"}}{As-received from the source system (WISKI export,
consultant report, etc.). Immutable once saved.}
\item{\code{"silver"}}{QC-reviewed: continuity checked, limb boundaries
verified, but not yet formally signed off.}
\item{\code{"gold"}}{Approved for production use. Default tier.}
}

Each call is assigned the next available version number for that
\code{station_id} \strong{within} the target tier. Existing versions are never
overwritten — the Parquet accumulates all sites and versions,
differentiated by \code{site_id} and \code{version}. The register records \code{tier}
so the full lineage (bronze → silver → gold) is traceable.

The ratings directory and register are created automatically if they
do not yet exist (no need to call \code{\link[=setup_hydro_store]{setup_hydro_store()}} first).
}
\section{Provenance register}{

Every saved curve gets a unique \code{rating_id} of the form
\verb{\{site_id\}_RATING_\{tier\}_v\{version\}} (e.g. \verb{510310_RATING_gold_v2}).
The register also records a \code{status} column. When a new version is saved,
any previously \code{"Active"} version for the same site and tier is
automatically updated to \code{"Superseded"}, so \code{list_ratings()} always
shows which version is currently in use.
}

\examples{
\dontrun{
# Bronze — as received from WISKI
save_rating(rc_raw, root = "data/hydro", tier = "bronze",
            saved_by = "J.Payne", method_of_receipt = "WISKI export",
            derivation_method = "spot gaugings",
            notes = "WISKI export 2023-11-01")

# Silver — after continuity check and correction
rc_fixed <- fix_limb_continuity(rc_raw)
save_rating(rc_fixed, root = "data/hydro", tier = "silver",
            saved_by = "J.Payne", method_of_receipt = "derived from bronze v1",
            derivation_method = "spot gaugings",
            notes = "Continuity corrected")

# Gold — modelled rating from a hydraulic model project
save_rating(rc_model, root = "data/hydro", tier = "gold",
            saved_by = "J.Payne", method_of_receipt = "ISIS 2D model output",
            derivation_method = "hydraulic model",
            model_or_project  = "TUFLOW_Phase2_2023",
            notes = "Approved by lead hydrologist")

# Gold — mixed: field gaugings calibrated against hydraulic model
save_rating(rc_fixed, root = "data/hydro", tier = "gold",
            saved_by = "J.Payne", method_of_receipt = "approved from silver v1",
            derivation_method = "mixed",
            model_or_project  = "Flood Risk Assessment 2023, Hydrology Team",
            notes = "Approved by lead hydrologist")

# Save an entire time-varying set in one call
save_rating(rating_set, root = "data/hydro", tier = "bronze",
            saved_by = "J.Payne", method_of_receipt = "WISKI export",
            derivation_method = "spot gaugings")
}
}
