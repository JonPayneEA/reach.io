% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating_store.R
\name{load_rating}
\alias{load_rating}
\title{Load rating curve(s) from the Gold calibration store}
\usage{
load_rating(site_id, root, tier = "gold", version = NULL)
}
\arguments{
\item{site_id}{Character. Station identifier (must match the \code{station_id}
used when the curve was saved).}

\item{root}{Character. Root of the data store.}

\item{tier}{Character. Tier to load from: \code{"bronze"}, \code{"silver"}, or
\code{"gold"} (default).}

\item{version}{Integer or \code{NULL}. When \code{NULL} (default), all versions are
loaded and returned as a \link{RatingSet} ordered by version number.
When a specific integer is given, only that version is returned as a
\link{RatingCurve}.}
}
\value{
A \link{RatingCurve} (single version) or \link{RatingSet} (all versions).
}
\description{
Reads the shared ratings Parquet, filters to \code{site_id}, and reconstructs
either one \link{RatingCurve} (when \code{version} is specified) or a \link{RatingSet}
containing every stored version for that site (default).
}
\examples{
\dontrun{
# Gold (default) — all versions as a RatingSet for apply_rating()
rs   <- load_rating("510310", root = "data/hydro")
flow <- apply_rating(level_obj, rs)

# Load from bronze to compare against gold
rc_bronze <- load_rating("510310", root = "data/hydro", tier = "bronze",
                         version = 1)
rc_gold   <- load_rating("510310", root = "data/hydro", version = 1)
}
}
