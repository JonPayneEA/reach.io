% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pe_sine.R
\name{regionalise_pe_sine}
\alias{regionalise_pe_sine}
\title{Regionalise PE sine parameters by pooling fitted gauges}
\usage{
regionalise_pe_sine(params_list, weights = NULL)
}
\arguments{
\item{params_list}{Named list of \code{\link{fit_pe_sine}} results, one per gauge.
\code{NULL} entries (failed fits) are silently dropped. Must contain at least
one non-NULL entry.}

\item{weights}{Numeric vector or \code{NULL}. Importance weights, one per entry
in \code{params_list} (including NULL entries, which are dropped before
weighting). Weights are normalised to sum to 1. If \code{NULL}, equal weighting
is used. Typical choices: catchment area (km\eqn{^2}), inverse distance to
the ungauged site, or climatological similarity.}
}
\value{
A named list with elements:
\describe{
  \item{\code{B}}{Weighted mean annual PE (mm day\eqn{^{-1}}).}
  \item{\code{A}}{Weighted mean amplitude (mm day\eqn{^{-1}}).}
  \item{\code{phi}}{Phase-aware weighted mean phase (day of year).}
  \item{\code{n_gauges}}{Number of gauges used after dropping NULLs.}
  \item{\code{gauge_ids}}{Names of the gauges used, from \code{names(params_list)}.}
}

Pass the returned \code{B}, \code{A}, \code{phi} directly to
\code{\link{generate_pe_sine}} to produce a synthetic PE series for the
ungauged catchment.
}
\description{
Combines \code{\link{fit_pe_sine}} results from multiple gauges into a single set
of regional parameters, using weighted or equal averaging. Useful for
estimating PE at ungauged catchments by pooling neighbouring gauges.

Phase (\eqn{\phi}) averaging is performed on the unit circle to avoid
wrap-around artefacts when gauges span the day 0/365 boundary (e.g.
mixing a gauge with \eqn{\phi = 5} and one with \eqn{\phi = 360}).
}
\examples{
dates  <- seq(as.Date("2010-01-01"), as.Date("2019-12-31"), by = "day")
doy    <- as.integer(format(dates, "\%j"))

make_pe <- function(B, A, phi)
  B + A * sin(2 * pi / 365.25 * (doy - phi))

fits <- list(
  gauge_A = fit_pe_sine(make_pe(2.4, 1.9, 170), dates),
  gauge_B = fit_pe_sine(make_pe(2.6, 2.1, 174), dates),
  gauge_C = fit_pe_sine(make_pe(2.5, 2.0, 172), dates)
)

# Equal weighting
regional <- regionalise_pe_sine(fits)

# Area weighting (catchment areas in km2)
regional_w <- regionalise_pe_sine(fits, weights = c(120, 85, 200))

# Generate synthetic PE for the ungauged site
pe_ungauged <- generate_pe_sine(regional_w$B, regional_w$A, regional_w$phi,
                                start = "2020-01-01", end = "2024-12-31")
}
\seealso{
\code{\link{fit_pe_sine}}, \code{\link{generate_pe_sine}}
}
