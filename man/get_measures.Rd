% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup.R
\name{get_measures}
\alias{get_measures}
\title{Get measure metadata from the EA Hydrology API}
\usage{
get_measures(
  parameter = c("rainfall", "flow", "level"),
  period_name = NULL,
  value_type = NULL,
  limit = 5000
)
}
\arguments{
\item{parameter}{Character. One of \code{"rainfall"}, \code{"flow"}, or \code{"level"}.}

\item{period_name}{Character or \code{NULL}. \code{"daily"} or \code{"15min"}. Defaults
to the parameter's built-in default (daily for all three).}

\item{value_type}{Character or \code{NULL}. Value statistic filter, e.g.
\code{"mean"}, \code{"min"}, \code{"max"}, \code{"instantaneous"}, \code{"total"}. \code{NULL} uses
the parameter default. \code{"all"} skips the filter entirely.}

\item{limit}{Integer. Maximum number of measures to return. Default 2000.}
}
\value{
A \code{data.table} of measure metadata. Key columns include
\code{notation} (the measure ID used in readings requests),
\code{station} (full station URI), \code{station.label}, \code{station.wiskiID},
\code{periodName}, \code{unitName}, \code{valueType}, and \code{parameter}.
}
\description{
Fetches the full list of available measurement time series for a given
parameter type, filtered by temporal resolution and optionally by value
statistic. Returns a \code{data.table} that includes \code{station.wiskiID} and
\code{station} (the full station URI), which can be used to filter to specific
stations after fetching rather than making multiple per-station API calls.
}
\details{
For the common case where you already know which stations you want,
\code{find_stations()} returns measure notations directly in its \code{measures}
list column, avoiding a second API call entirely.
}
\examples{
\dontrun{
# All daily mean flow measures
all_flow <- get_measures("flow")

# Filter to specific stations by WISKI ID after fetching
all_flow[`station.wiskiID` \%in\% c("SS92F014", "S11512_FW")]

# All 15-minute rainfall measures
get_measures("rainfall", period_name = "15min")

# All level statistics (min, max, instantaneous)
get_measures("level", value_type = "all")
}
}
