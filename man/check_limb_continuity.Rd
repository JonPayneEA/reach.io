% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rating.R
\name{check_limb_continuity}
\alias{check_limb_continuity}
\title{Check that rating limbs connect at their shared boundaries}
\usage{
check_limb_continuity(x, ...)
}
\arguments{
\item{x}{A \link{RatingCurve}.}

\item{tol_pct}{Numeric scalar. Percentage tolerance for declaring a junction
connected. Default \code{1} (i.e. 1 \%).}
}
\value{
A \code{data.table} (returned invisibly) with one row per junction and
columns:
\describe{
\item{\code{junction}}{Label, e.g. \code{"1-2"}.}
\item{\code{h_junction}}{Stage at the boundary (m).}
\item{\code{Q_lower_limb}}{Q from the lower limb at the boundary (m³/s).}
\item{\code{Q_upper_limb}}{Q from the upper limb at the boundary (m³/s).}
\item{\code{gap_m3s}}{Signed difference \code{Q_upper - Q_lower} (m³/s).}
\item{\code{gap_pct}}{Relative gap as a percentage of \code{Q_lower_limb}.}
\item{\code{connected}}{Logical. \code{TRUE} when \code{abs(gap_pct)} <= \code{tol_pct}.}
}
}
\description{
At each junction between limb \eqn{i} and limb \eqn{i+1}, evaluates the
rating equation from both limbs at the shared boundary stage and reports
the absolute and relative Q difference. A junction is considered connected
when the relative gap is within \code{tol_pct}.
}
\details{
Use \code{\link[=fix_limb_continuity]{fix_limb_continuity()}} to correct any discontinuities by adjusting
the \code{C} parameters.
}
\examples{
\dontrun{
check_limb_continuity(rc)
check_limb_continuity(rc, tol_pct = 0.5)
}
}
