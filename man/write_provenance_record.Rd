% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{write_provenance_record}
\alias{write_provenance_record}
\title{Write a Bronze provenance record to the Hydrometric Data Register}
\usage{
write_provenance_record(
  register_path,
  dataset_id,
  supplier,
  supplier_code,
  site_id,
  data_type,
  time_period_start,
  time_period_end,
  temporal_resolution,
  method_of_receipt,
  file_path,
  received_by = Sys.info()[["user"]],
  units = NA_character_,
  supplier_quality_flags = NA_character_,
  known_limitations = NA_character_,
  notes = NA_character_
)
}
\arguments{
\item{register_path}{Character. Path to the register CSV file. Typically
\code{hydrometric/register/hydrometric_data_register.csv}.}

\item{dataset_id}{Character. Bronze dataset ID.}

\item{supplier}{Character. Supplier name, e.g. \code{"Environment Agency"}.}

\item{supplier_code}{Character. Supplier code, e.g. \code{"EA"}.}

\item{site_id}{Character. Gauge or site identifier.}

\item{data_type}{Character. Framework data type code.}

\item{time_period_start}{Character. Start date of the dataset (YYYY-MM-DD).}

\item{time_period_end}{Character. End date of the dataset (YYYY-MM-DD).}

\item{temporal_resolution}{Character. Time step, e.g. \code{"15min"}, \code{"daily"}.}

\item{method_of_receipt}{Character. How the data was obtained, e.g.
\code{"API pull"}, \code{"FTP"}, \code{"bulk file"}, \code{"WISKI .all export"}.}

\item{file_path}{Character. Storage path of the Bronze Parquet file.}

\item{received_by}{Character. Name of the Custodian who ingested the data.
Defaults to \code{Sys.info()[["user"]]}.}

\item{units}{Character or NULL. Units as stated by the supplier.}

\item{supplier_quality_flags}{Character or NULL. Description of any quality
codes provided by the supplier.}

\item{known_limitations}{Character or NULL. Limitations stated by supplier.}

\item{notes}{Character or NULL. Any unusual aspects of the delivery.}
}
\value{
The provenance record as a one-row \code{data.table}, invisibly.
}
\description{
Appends a provenance record row to the register CSV file for the given
Bronze dataset. This satisfies the Section 4.2 requirement that every
Bronze dataset has a complete provenance record before storage.
}
\details{
The register CSV is created if it does not exist. Each row represents
one Bronze dataset following the field list in Section 10.1 of the
framework.
}
\examples{
\dontrun{
write_provenance_record(
  register_path       = "data/hydrometric/register/register.csv",
  dataset_id          = "EA_39001_Q_20260115",
  supplier            = "Environment Agency",
  supplier_code       = "EA",
  site_id             = "39001",
  data_type           = "Q",
  time_period_start   = "2020-01-01",
  time_period_end     = "2024-12-31",
  temporal_resolution = "15min",
  method_of_receipt   = "API pull",
  file_path           = "data/hydrometric/bronze/EA/Q/2026/EA_39001_Q_20260115.parquet"
)
}
}
