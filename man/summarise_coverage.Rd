% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{summarise_coverage}
\alias{summarise_coverage}
\title{Summarise date coverage for Bronze store gauges}
\usage{
summarise_coverage(root, site_id = NULL, category = NULL, data_type = NULL)
}
\arguments{
\item{root}{Character. Root of the data store.}

\item{site_id}{Character vector or \code{NULL}. Filter to one or more sites.
\code{NULL} (default) shows all sites.}

\item{category}{Character vector or \code{NULL}. Filter to one or more
categories. \code{NULL} shows all.}

\item{data_type}{Character vector or \code{NULL}. Filter to one or more data
type codes. \code{NULL} shows all.}
}
\value{
A \code{data.table} with one row per site + data-type, printed as a
coverage summary. Returned invisibly.
}
\description{
Reports the time span, record count, and completeness percentage for each
gauge in the Bronze store.  Completeness is estimated from the register
(\code{time_period_start} / \code{time_period_end} / \code{temporal_resolution}) without
reading the underlying Parquet files, so it reflects the \emph{intended} coverage
per ingest batch rather than the literal row count.
}
\details{
To inspect actual record counts or detect individual missing timesteps, use
\code{\link[=check_gaps]{check_gaps()}}.
}
\examples{
\dontrun{
# Coverage for all gauges
summarise_coverage("data/hydro")

# Flow coverage for a specific site
summarise_coverage("data/hydro", site_id = "39001", data_type = "Q")
}
}
